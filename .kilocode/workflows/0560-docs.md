<!-- Generated by ACOS. DO NOT EDIT.
Source: .ssot/agents/entrypoints/0560-docs.md
-->

# 0560-docs

> Generate or update project documentation verified against the codebase

> Generalist, project-agnostic. Project identity is read from
> `.ssot/context-index.md` and `.ssot/agents/clients.json` at runtime.
> ## You are in an ACOS project
> ACOS maintains a single source of truth under `.ssot/` and projects it to
> multiple AI clients. You are one of those clients.
> ## Orientation sequence (read before acting)
> 1. **Project identity** — `.ssot/context-index.md` (name, role, specs, sources)
> 2. **Current state** — `.ssot/status.md` (objective, milestone, open items)
> 3. **Next action** — `.ssot/handoff.md` (recommended next action)
> 4. **Ecosystem** — if `clients.json` declares `ecosystemParent`, read parent
>    `.ssot/context-index.md` and `.ssot/status.md`
> 5. **Authority** — `.ssot/protected-paths.json` (edit vs managed)
> 6. **Entry points** — context-index "Common entrypoints" = command surface
> 7. **Prefix standardization** — before adding a custom entrypoint, check whether the need is
>    already covered by a core category in `000-numbering-conventions/spec.md`; if not, classify it
>    as `project` (1500+) or, when `clients.json` declares an ecosystem parent/children, `ecosystem` (13xx–14xx).
> ## When you are a sub-agent
> You do NOT share the calling agent's context. Re-orient using the sequence
> above. Your prompt has `{{TASK}}` and `{{PROJECT_PATH}}` — use them as
> objective and working directory, but read `.ssot/` for everything else.

<!-- ACOS-ORIENTATION:START -->
> **Orientation**: Read `.ssot/context-index.md`, `.ssot/status.md`, and
> `.ssot/handoff.md` before proceeding. Full reference:
> `.ssot/agents/context/orientation.md`.
<!-- ACOS-ORIENTATION:END -->

Generate or update project documentation, verified against the actual codebase.

1. Determine the documentation scope from the user's request (README, API docs, ADRs, architecture overview).
2. Read the relevant source files and existing docs. Cross-check every claim against the code — never document behavior that the code does not exhibit.
3. For API docs: extract signatures, types, and examples from the source. Do not invent endpoints or parameters.
4. For ADRs: record the decision, its context, alternatives considered, and consequences. Number sequentially.
5. Produce or update the documentation files in their conventional locations.
6. Produce `specs/<feature-id>/docs.md` with a summary of files created or updated and verification notes (which claims were checked against which source files).
7. Do not delete existing user-authored documentation. Preserve project-specific content and only update the sections that are stale.
