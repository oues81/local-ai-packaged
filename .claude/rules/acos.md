<!-- Generated by ACOS. DO NOT EDIT.
Source: .ssot/agents/rules/common.md and .ssot/protected-paths.json
-->

# ACOS Project Rules

1. Canonical harness sources live under `.ssot/`; files carrying the ACOS generated banner are projections and are never edited directly.
2. Stable workflow IDs are used in dependencies and documentation. Numeric prefixes control visible ordering only.
3. Preserve unrelated user changes and do not perform destructive or externally visible actions without authority.
4. Read `.ssot/status.md` and `.ssot/handoff.md` when resuming work; update them after meaningful progress.
5. Run `npx --no-install acos --fix`, `npx --no-install acos --check`, and relevant project verification after changing canonical harness sources. Prefer `maintenance.sync` / `0660-sync` for projection regeneration and `maintenance.doctor` / `0640-doctor` when drift is suspected.
6. Treat `.ssot/protected-paths.json` as advisory unless the active client adapter explicitly reports native enforcement.
7. Maintain `.ssot/context-index.md` as a compact, canonical map of active specifications, architecture, and key project context. Link to sources rather than duplicating them.

## Canonical lifecycle hooks

These hooks are semantic requirements. A client with a safe native hook surface may enforce them automatically; every other adapter projects them into its mandatory rules and explicit entrypoints.

- On entrypoint activation: orient from `.ssot/context-index.md`, `.ssot/status.md`, and `.ssot/handoff.md` before executing the entrypoint body. Sub-agents re-orient independently — they do not inherit the calling agent's context.
- Before mutation: inspect Git state, protected paths, applicable specifications, and the authority boundary.
- After mutation: run the smallest relevant static checks and tests, then inspect generated-file drift.
- Before completion: run `npx --no-install acos --check` when the harness is present, report checks not run, and persist `.ssot/status.md` plus `.ssot/handoff.md` after meaningful progress.
- On session resume: read durable status and handoff before selecting the next action.
- On infrastructure use: discover the configured external profile first; never deploy a substitute because discovery failed.
- On native client verification: prefer mechanical checks over live sessions; warn the user and obtain explicit approval before launching more than one live `0020-resume` session in a batch; record the date, client, and version in the native verification checklist after any live session.


# External runtime rule

Git is the single source of truth for any declared external runtime in `.ssot/agents/runtimes.json`. The runtime is derived from the repository; agents MUST NOT edit the runtime without a prior committed git change.

- Before mutating a runtime, verify the current drift status with `npx --no-install acos-runtime-sync --root .`.
- If the status is `runtime-diverged`, stop and ask the user to reconcile git and the runtime before continuing.
- Only execute the declared `syncCommand` when the user explicitly requests it via `npx --no-install acos-runtime-sync --root . --sync`.
- Never run runtime drift, health, or sync commands automatically from `0020-resume` or any other entrypoint.

## Notes for Claude Code

These rules are projected to `.claude/rules/acos.md` and loaded automatically by Claude Code for every session.

- Optional YAML frontmatter can scope the rule to specific paths using `paths` globs.
- Keep the shared ACOS rules canonical; add only client-specific notes here.

## Projected path policy

Enforcement mode: **advisory**.

- `.ssot/**`: **ask** for delete — Canonical ACOS state may be edited deliberately but must not be deleted casually.
- `AGENTS.md`: **deny** for write, delete — Managed projection of .ssot/agents/context/AGENTS.src.md.
- `CLAUDE.md`: **deny** for write, delete — Managed projection of .ssot/agents/context/CLAUDE.src.md.
- `.claude/skills/[0-9][0-9][0-9]-*/**`: **deny** for write, delete — Managed ACOS skill projection.
- `.claude/commands/[0-9][0-9][0-9]-*.md`: **deny** for write, delete — Managed ACOS command projection for Claude Code.
- `.cursor/commands/[0-9][0-9][0-9]-*.md`: **deny** for write, delete — Managed ACOS command projection.
- `.cursor/skills/*/**`: **deny** for write, delete — Managed ACOS skill projection for Cursor.
- `.cursor/rules/acos.mdc`: **deny** for write, delete — Managed ACOS rule projection.
- `.kilo/skills/[0-9][0-9][0-9]-*/**`: **deny** for write, delete — Managed ACOS skill projection for Kilo Code.
- `.kilo/commands/[0-9][0-9][0-9]-*.md`: **deny** for write, delete — Managed ACOS command projection for current Kilo surfaces.
- `.kilocode/workflows/[0-9][0-9][0-9]-*.md`: **deny** for write, delete — Managed ACOS workflow projection.
- `.kilocode/rules/acos.md`: **deny** for write, delete — Managed ACOS rule projection.
- `.opencode/commands/[0-9][0-9][0-9]-*.md`: **deny** for write, delete — Managed ACOS command projection.
- `.opencode/skills/*/**`: **deny** for write, delete — Managed ACOS skill projection for OpenCode.
- `.claude/rules/*.md`: **deny** for write, delete — Managed ACOS rule projection for Claude Code.
- `.kilo/rules/*.md`: **deny** for write, delete — Managed ACOS rule projection for current Kilo Code surfaces.
- `kilo.jsonc`: **deny** for write, delete — Managed ACOS Kilo Code config projection.
- `.opencode/rules/*.md`: **deny** for write, delete — Managed ACOS rule projection for OpenCode.
- `opencode.json`: **deny** for write, delete — Managed ACOS OpenCode config projection.
- `.devin/skills/[0-9][0-9][0-9]-*/**`: **deny** for write, delete — Managed ACOS skill projection for Devin CLI.
- `.windsurf/workflows/[0-9][0-9][0-9]-*.md`: **deny** for write, delete — Managed ACOS workflow projection for Devin Desktop using its legacy storage namespace.
- `.agents/skills/[0-9][0-9][0-9]-*/**`: **deny** for write, delete — Managed ACOS skill projection.
- `.codex/skills/[0-9][0-9][0-9]-*/**`: **deny** for write, delete — Managed ACOS skill projection for OpenAI Codex.
- `.claude/agents/*.md`: **deny** for write, delete — Managed ACOS agent projection for Claude Code.
- `.cursor/agents/*.md`: **deny** for write, delete — Managed ACOS agent projection for Cursor.
- `.codex/agents/*.toml`: **deny** for write, delete — Managed ACOS agent projection for OpenAI Codex.
- `.opencode/agents/*.md`: **deny** for write, delete — Managed ACOS agent projection for OpenCode.
- `.kilo/agents/*.md`: **deny** for write, delete — Managed ACOS agent projection for Kilo Code.
- `.claude/settings.json`: **deny** for write, delete — Managed ACOS hooks projection for Claude Code.
- `.cursor/hooks.json`: **deny** for write, delete — Managed ACOS hooks projection for Cursor.
- `.codex/hooks.json`: **deny** for write, delete — Managed ACOS hooks projection for OpenAI Codex.
- `.devin/hooks.v1.json`: **deny** for write, delete — Managed ACOS hooks projection for Devin.
- `.mcp.json`: **deny** for write, delete — Managed ACOS MCP projection for Claude Code.
- `.cursor/mcp.json`: **deny** for write, delete — Managed ACOS MCP projection for Cursor.
- `.devin/config.json`: **deny** for write, delete — Managed ACOS MCP projection for Devin.
- `.codex/config.toml`: **deny** for write, delete — Managed ACOS MCP projection for OpenAI Codex.
- `ACOS_RUNTIME_SYNC_REPORT.json`: **deny** for write, delete — Generated by acos-runtime-sync; refresh it with the command, do not edit manually.
- `.env*`: **deny** for read, write, delete — Potential secrets and environment variables.
- `agent/instructions/*.md`: **deny** for write, delete — Managed ACOS projection for Eve.
- `agent/skills/*/SKILL.md`: **deny** for write, delete — Managed ACOS projection for Eve.
- `agent/connections/*.ts`: **deny** for write, delete — Managed ACOS projection for Eve.
- `agent/hooks/acos-*.ts`: **deny** for write, delete — Managed ACOS projection for Eve.
- `agent/sandbox/workspace/.ssot-seed-manifest.json`: **deny** for write, delete — Managed ACOS projection for Eve.
- `.devin/agents/*/AGENT.md`: **deny** for write, delete — Managed ACOS agent projection for Devin.
- `.agents/agents/*/AGENT.md`: **deny** for write, delete — Managed ACOS agent projection (shared .agents standard).
- `.devin-plugin/plugin.json`: **deny** for write, delete — Managed ACOS plugin projection for Devin.
- `.codex/rules/*.rules`: **deny** for write, delete — Managed ACOS rules projection for OpenAI Codex.
- `.codex-plugin/plugin.json`: **deny** for write, delete — Managed ACOS plugin projection for OpenAI Codex.
- `.kilo/plugin/acos-hooks.ts`: **deny** for write, delete — Managed ACOS hooks projection for Kilo Code.
- `.kilo/tools/*.ts`: **deny** for write, delete — Managed ACOS tools projection for Kilo Code.
- `.opencode/plugins/acos-hooks.ts`: **deny** for write, delete — Managed ACOS hooks projection for OpenCode.
- `.opencode/tools/*.ts`: **deny** for write, delete — Managed ACOS tools projection for OpenCode.
- `.claude/workflows/*.js`: **deny** for write, delete — Managed ACOS workflow projection for Claude Code.
- `.claude-plugin/plugin.json`: **deny** for write, delete — Managed ACOS plugin projection for Claude Code.
- `.claude/output-styles/*.md`: **deny** for write, delete — Managed ACOS output-style projection for Claude Code.
- `.cursor/permissions.json`: **deny** for write, delete — Managed ACOS permissions projection for Cursor.
- `.cursor/cli.json`: **deny** for write, delete — Managed ACOS CLI config projection for Cursor.
- `.cursor-plugin/plugin.json`: **deny** for write, delete — Managed ACOS plugin projection for Cursor.
- `agent/tools/*.ts`: **deny** for write, delete — Managed ACOS tools projection for Eve.
- `agent/subagents/*/**`: **deny** for write, delete — Managed ACOS subagent projection for Eve.
- `.ssot/knowledge/**`: **ask** for delete — OKF knowledge layer concept pages — canonical content, do not delete casually.
- `.ssot/knowledge-profile.json`: **ask** for delete — OKF knowledge layer profile — canonical configuration.
