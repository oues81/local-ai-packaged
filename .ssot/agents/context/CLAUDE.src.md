# My Project — Claude Code Context

This file is a Claude-specific pointer. The shared canonical source is `/.ssot/agents/context/AGENTS.src.md`.

## Read first

1. `/AGENTS.md`
2. `/.ssot/agents/README.md`
3. `/.ssot/protected-paths.json`

## Quick orientation

When activating any entrypoint:
1. Read `.ssot/context-index.md` for project overview
2. Read `.ssot/status.md` for current state
3. Read `.ssot/handoff.md` for next action
4. Apply any entrypoint-specific orientation if declared

Full reference: `.ssot/agents/context/orientation.md`

## Invariant

The multi-client harness is maintained once in `.ssot/agents/`. Claude Code is no exception.

## Reminders

- Do not edit generated files directly.
- Run `/0660-sync` after changing `.ssot/agents/`.
- Log every durable decision in `.ssot/decisions.md`.
