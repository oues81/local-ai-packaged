## Notes for Kilo Code

These rules are projected to `.kilo/rules/acos.md` and loaded by the `instructions` array in `kilo.jsonc`.

- The legacy `.kilocode/rules/acos.md` path remains for backward compatibility.
- Keep the shared ACOS rules canonical; add only client-specific notes here.
