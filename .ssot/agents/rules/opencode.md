## Notes for OpenCode

These rules are projected to `.opencode/rules/acos.md` and referenced from the generated `opencode.json` `instructions` array.

- OpenCode also reads `AGENTS.md` as project-level rules.
- Keep the shared ACOS rules canonical; add only client-specific notes here.
